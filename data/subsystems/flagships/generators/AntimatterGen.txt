Subsystem: AntimatterGen
	Name: #AMGEN_NAME
	Description: #AMGEN_DESC
	BaseColor: #5ade8b
	Elevation: 1

	Tags: IsReactor, Category:Power 
	Hull: Flagship, Station

	Size := HexSize * Hexes
	Power := 40 * Size
	EnergyCapacity := Power * 5
	SupplyCapacity := Size * 1200
	SupplyRate := Size * 125

	Hex.Resistance := 0.5
	Hex.HP := 12 * HexSize
	Hex.Mass := 3.5 * HexSize

	Hex.LaborCost := 0.4 * HexSize + 1
	Hex.BuildCost := 12 * HexSize + 4
	Hex.MaintainCost := 40 //discourage the use in small ships

	Provides:
		Power = Power

	Module: Core
		Sprite: AntimatterGen

	Module: Default

	Modifier: SupplyCapacityFactor(factor)
		SupplyCapacity := SupplyCapacity * factor

	Modifier: PowerFactor(factor)
		Power := Power * factor
