Subsystem: ShieldedAntimatter
	Name: #S_SHIELDED_AMGEN
	Description: #S_SHIELDED_AMGEN_DESC
	BaseColor: #5ade8b
	Elevation: 1

	Tags: IsReactor, Category:Power
	Hull: Flagship, Station

	Size := HexSize * Hexes
	Power := 12 * Size
	EnergyCapacity := Power * 5
	SupplyCapacity := Size * 800
	SupplyRate := Size * 75
	CombatSupplyRate := SupplyRate / 10

	Hex.Resistance := 0.5
	Hex.HP := 20 * HexSize
	Hex.Mass := 4 * HexSize

	Hex.LaborCost := 0.8 * HexSize + 10
	Hex.BuildCost := 13 * HexSize + 10
	Hex.MaintainCost := 6*HexSize+5

	Hook: ResupplyFlagship(CombatSupplyRate, In Combat = True)
	Provides:
		Power = Power

	Module: Core
		Sprite: SAmatter
	Module: Default

	Modifier: SupplyCapacityFactor(factor)
		SupplyCapacity := SupplyCapacity * factor

	Modifier: PowerFactor(factor)
		Power := Power * factor
